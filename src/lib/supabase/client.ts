// Browser-side Supabase client placeholder
// In production, replace with actual Supabase client
export function createClient() {
  // Demo mode - return null, auth is handled via localStorage
  return null;
}
